# ========================================
# Configuration des dates
# ========================================
# Date de début (format: YYYY-MM)
START_DATE=2025-01

# Date de fin (optionnel, si vide = aujourd'hui)
# Laissez vide pour télécharger jusqu'à aujourd'hui
END_DATE=

# ========================================
# Configuration Azure Blob Storage (optionnel)
# ========================================
# Si ces variables ne sont pas définies, les fichiers seront téléchargés localement
# dans le dossier data/raw/

# Connection string Azure Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account;AccountKey=your_key;EndpointSuffix=core.windows.net

# Nom du container Azure
AZURE_CONTAINER_NAME=raw

# ========================================
# Configuration PostgreSQL (Cosmos DB)
# ========================================
POSTGRES_HOST=your-cosmos-db.postgres.database.azure.com
POSTGRES_PORT=5432
POSTGRES_DB=taxi_data
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password

# SSL Mode (pour Cosmos DB, utilisez 'require')
POSTGRES_SSL_MODE=require
